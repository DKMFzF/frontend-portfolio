import { FC } from 'react';
import style from './section-stack.module.scss';

export const SectionStackUI: FC = () => (
	<section id='SectionStackUI'>
		<span className={style.text}>Страница пока что в разработке :)</span>
	</section>
);
