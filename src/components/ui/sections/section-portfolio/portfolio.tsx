import { FC } from 'react';
import { SectionProfileUIProps } from './type';

export const SectionPortfolioUI: FC<SectionProfileUIProps> = () => (
	<section id='SectionPortfolioUI'>Привет мир!</section>
);
