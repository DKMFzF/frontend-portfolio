import { CommonPage } from '@ui';
import styles from './Sketches.module.scss';

export const Sketches = () => (
	<>
		<CommonPage pageStyles={styles.sketches}>Привет мир!</CommonPage>
	</>
);

export default Sketches;
