# include:
#   - local: .gitlab-ci/lint.yml
#   - local: .gitlab-ci/test.yml

stages:
  - lint
  - test

lint:
  stage: lint
  image: node:20
  tags: [nodejs-docker]
  script:
    - npm i --force
    - npm run lint
